<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
  </head>
  <body>
    <div>
      <h1>Users page</h1>

      <ul>
        <% if (prods.length > 0) { %> <% prods.forEach(product => { %>
        <li><%= product.title %></li>
        <% }) %> <% } else { %>
        <li>No users found!</li>
        <% } %>
      </ul>
    </div>
  </body>
</html>
